#include <stdio.h>

int main()
{
    // For point value of SI we use float
    // float p;
    // p = 195;
    // int r;
    // r = 6;
    // int t;
    // t = 3;
    // printf("The simple interest is %f",(p*r*t)/100);
    // for round off value if SI we use int
    int p = 200;
    int r = 5;
    int t = 2;
    printf("The value of Simple interest is %d", (p * r * t) / 100);

        return 0;  
}