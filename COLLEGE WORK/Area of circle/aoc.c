#include <stdio.h>

int main()
{
    float a = 3.14;
    int r;
    printf("Enter the radius of the circle r ", r);
    scanf("%d", &r);
    printf("The area of the cirlce is %f", a * r * r);
    return 0;
}
