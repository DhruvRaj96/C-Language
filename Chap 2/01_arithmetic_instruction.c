#include <stdio.h>

int main(){
    int a = 9;
    int b = 3;
    int c = a + b; // a and b are the operands and +,= are the operators and c is the result 
    printf("The value of a is %d and the value of b is %d and the value of c is the sum of  and  is %d\n",a ,b,c);
    // If we have to find the remainder of any division we must have to use MODULUS OPERATOR
    printf("The remainder when a is divided by b is: %d\n",a%b);
    return 0;
}