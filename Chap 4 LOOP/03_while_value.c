#include <stdio.h>

int main()
{
    int i = 0;
    while (i < 8)
    {
        printf("The Value Of i Is %d\n", i);
        // i = i + 1; It can also be written as i++
        i++;
    }
    return 0;
}